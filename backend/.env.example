# Database Configuration
# Copy this file to .env and update with your actual values

# For PythonAnywhere FREE account (MySQL):
# DATABASE_URL=mysql://YOUR_USERNAME:YOUR_PASSWORD@YOUR_USERNAME.mysql.pythonanywhere-services.com/YOUR_USERNAME$YOUR_DATABASE_NAME

# For PythonAnywhere PAID account (PostgreSQL):
# DATABASE_URL=postgresql://USERNAME:PASSWORD@HOSTNAME/DATABASE_NAME

# For local development (PostgreSQL):
DATABASE_URL=postgresql://pos_user:pos_password@localhost:5432/pos_db

# For local development (SQLite):
# DATABASE_URL=sqlite:///pos_database.db

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development

# Security
# Generate a secret key with: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=your-secret-key-here-change-in-production

# Notes:
# 1. NEVER commit the .env file to Git
# 2. Always use a strong SECRET_KEY in production
# 3. Set FLASK_ENV=production when deploying to PythonAnywhere
